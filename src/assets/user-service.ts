export function requestNewUser(name: string, email: string, password: string) {
    console.log(`User requested with name: ${name}, email: ${email} and password: ${password}`);
}

// Make a request for a user with a given ID
